\begin{center}
    \begin{tikzpicture}[grow cyclic, text width=2cm, align=flush center,
            font=\rmfamily, rotate=90,
            every node/.style={concept}, concept color=teal!100, text=white,
            level 1/.append style={level distance=4cm,sibling angle=180, concept color=teal!80},
            level 2/.append style={level distance=4cm,sibling angle=120, concept color=teal!70},
            model based/.append style={level distance=4cm,sibling angle=70, concept color=teal!70},
            level 3/.append style={level distance=4cm,sibling angle=70, concept color=teal!60},
            level 4/.append style={level distance=3cm,sibling angle=50, concept color=teal!50},
            deep RL/.append style={concept color = Orchid!100},]

        \node[text width=2.5cm]{Reinforcement learning}
        child { node {Model based}
                child [model based]{ node {MDP}
                        child[deep RL] { node {Actor critic}}
                    }
                child [concept color=gray!50]{
                        node {Nonlinear Dynamics}
                        child [deep RL] { node {Deep MPC}}
                        child [concept color=gray!50]{ node {Optimal control}}
                    }
                child [model based]{
                        node {DP}
                    }
            }
        child { node {Model free}
                child { node {Gradient free}
                        child { node {Off-policy}
                                child { node {Q-learning}}
                                child[deep RL] { node {DQN}}
                            }
                        child { node {On-policy}
                                child { node {SARSA}}
                                child { node {TD}}
                                child { node {$n$-step TD}}
                                child { node {Monte Carlo}}
                            }
                    }
                child { node {Gradient based}
                        child { node {Policy gradient optimization}}
                        child [deep RL]{ node {Deep policy networks}}
                    }
            }
        ;
    \end{tikzpicture}
\end{center}